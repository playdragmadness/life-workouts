// Copy this file to config.js, then replace both placeholders.
// The anon key is safe to use in the browser when Row Level Security is enabled.
window.APP_CONFIG = {
  SUPABASE_URL: "YOUR_PROJECT_URL",
  SUPABASE_ANON_KEY: "YOUR_ANON_KEY"
};
