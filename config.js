// ── Your Supabase connection settings ──────────────────────────────
// Fill in the anon key ONCE. This file is never regenerated, so any
// future updates to index.html will leave your keys untouched.
//
// The anon key is meant to live in the browser (row-level security is
// what protects your data), so it's safe to keep here and commit to Git.
// Never put the service_role key here.
//
// Find your anon key: Supabase dashboard → Project Settings → API →
// Project API keys → the one labeled "anon" / "public".
// ───────────────────────────────────────────────────────────────────

window.APP_CONFIG = {
  SUPABASE_URL: https://iltxpkbdktdkpubwkqcm.supabase.co,
  SUPABASE_ANON_KEY: sb_secret_-uoy-XQQwUXlvJgbl7d5Zw_uHnUY7jf
};
